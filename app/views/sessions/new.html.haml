= provide(:title, "Sign In")

.jumbotron
  %h1 Welcome to Service KPI Analysis from Saluma Solutions Ltd.

  %p Your "everything you need to know about Service on one page" tool for Dealers and General Managers.


- flash.each do |key, msg|
  %div{ :class => key } 
    #{msg}

= form_for(:session, url: sessions_path) do |f|
  .container
    = f.label(:email, "Email: ")
    .input-group
      %span.input-group-addon @
      = f.text_field(:email, placeholder: "sample@email.com", :class => 'form-control')

    = f.label(:password, "Password: ")
    .input-group
      %span.input-group-addon *
      = f.password_field(:password, placeholder: "your password", :class => 'form-control')

    = f.submit "Log In", :class => 'btn btn-primary btn-lg'